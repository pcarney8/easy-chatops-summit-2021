# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

namespace: pro
targetRevision: v1.1.0-branch #this is for helm configs
imageTag: &tag v1.1.0 #this is for container image
valueFile: values-pro.yaml
parameters:
  - name: image.tag
    value: *tag
