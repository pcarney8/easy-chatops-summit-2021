# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

namespace: pro
targetRevision: v3.0.0-branch #this is for helm configs
imageTag: &tag v3.0.0 #this is for container image
valueFile: values-pro.yaml
autoSync: false
parameters:
  - name: image.tag
    value: *tag
