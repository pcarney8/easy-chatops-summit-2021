# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

namespace: demo
targetRevision: v4.1.0-branch #this is for helm configs
imageTag: &tag v4.1.0 #this is for container image
parameters:
  - name: image.tag
    value: *tag
    #TODO OVERRIDE THINGS IN VALUES.YAML INSTEAD OF USING VALUES-CI.YAML
