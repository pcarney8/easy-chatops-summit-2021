# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

namespace: demo
targetRevision: v1.0.1-branch #this is for helm configs
imageTag: &tag v1.0.1 #this is for container image
parameters:
  - name: image.tag
    value: *tag
    #TODO OVERRIDE THINGS IN VALUES.YAML INSTEAD OF USING VALUES-CI.YAML
